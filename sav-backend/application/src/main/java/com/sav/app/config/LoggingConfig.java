package com.sav.app.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class LoggingConfig {
    // Logging configuration is handled by application.yml
    // This class can be extended with custom logging beans if needed
}
